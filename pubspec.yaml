name: pragma_test
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.0.1
  http: ^0.13.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  mocktail: ^0.3.0
  test: ^1.16.0
  bloc_test: ^9.0.0
  equatable: ^2.0.3
  network_image_mock: ^2.0.1

flutter:

  uses-material-design: true

  assets:
    - assets/images/cat_no_image.png
